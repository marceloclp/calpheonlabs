interface:
    display_name: "Migrate Table Decoder"
    short_description: "Port legacy BDO tables to verified BSD schemas"
    default_prompt: "Use $migrate-table to port a decoded BDO table into calpheonlabs with BSD primitives and exact snapshot parity."
